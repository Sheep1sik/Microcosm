// Secrets.xcconfig.template
//
// 이 파일을 복사해 같은 디렉토리에 `Secrets.xcconfig` 로 저장한 뒤
// 실제 키 값을 채워 넣으세요. 실제 `Secrets.xcconfig` 는 .gitignore 됩니다.
//
// CI 환경에서는 워크플로가 stub 값으로 자동 생성합니다.

OPENAI_API_KEY = your_openai_api_key_here
