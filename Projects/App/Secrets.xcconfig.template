// Secrets.xcconfig.template
//
// ⚠️ 임시 구조 — 클라이언트 직접 호출용
// 현재 OPENAI_API_KEY 는 빌드 타임에 Info.plist 로 embed 되어
// 앱 번들(.ipa)에 그대로 포함됩니다. 배포된 빌드에서 누구나 추출 가능합니다.
//
// 이 파일은 **로컬 개발 중 OpenAI 호출을 수동 테스트할 때만** 사용합니다.
// 실제 배포 빌드에서는 stub 값을 유지하고, OpenAI 호출은 비활성화/우회하세요.
//
// 장기 해결: Firebase Functions 등 서버 프록시로 이전 (이슈: security/openai-key-proxy)
//
// 사용법:
// 1. 이 파일을 복사해 같은 디렉토리에 `Secrets.xcconfig` 로 저장
// 2. 실제 키 값을 채워 넣음 (실제 파일은 .gitignore 됨)
// 3. CI 환경에서는 워크플로가 stub 값으로 자동 생성

OPENAI_API_KEY = your_openai_api_key_here
